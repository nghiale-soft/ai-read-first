# File Workflow Verification

Verify meaningful file behavior:

```text
upload/input
→ validation
→ storage/processing
→ metadata
→ retrieval/download
→ content and state assertion
```

Include relevant cases such as empty file, large file, Unicode filename/content, duplicate/overwrite, timeout, partial failure, and cleanup.
