# Web E2E

Reuse the configured framework. For a new setup, prefer Playwright unless project constraints indicate otherwise.

Cover critical journeys, not every visual detail.

Typical assertions:

- user action succeeds
- API result is reflected in UI
- persisted state is correct
- authorization and validation failures are handled
- no silent frontend error occurs
