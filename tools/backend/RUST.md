# Rust

```bash
cargo fmt --check
cargo clippy --all-targets --all-features
cargo check
cargo test <relevant-test>
```

Use an integration or executable domain workflow test for changed business behavior.
