# PHP

Typical configured tools:

```bash
vendor/bin/php-cs-fixer fix
vendor/bin/phpstan analyse
vendor/bin/phpunit
```

Use HTTP/application integration or domain workflow tests for business verification.
