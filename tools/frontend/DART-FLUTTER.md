# Dart and Flutter

```bash
dart format <changed-files>
flutter analyze
flutter test <relevant-tests>
```

For business workflows use Flutter `integration_test` or the project's existing E2E framework.

Do not run a platform build unrelated to the changed target unless impact requires it.
